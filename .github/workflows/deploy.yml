name: deploy

on: 
    push:
        branches:
            - main

jobs:
    deploy:
        name: Deploy
        runs-on: ubuntu-latest
        steps:
            - name: execute ssh command
              uses: appleboy/ssh-action@v1.2.1
              with:
                host:  ${{ secrets.SSH_HOST }}
                username: ${{ secrets.SSH_USER }}
                password: ${{ secrets.SSH_PWD }}
                fingerprint: qUA9dqfFxy9NFGLz+Eo3UUqlu0w5NrCJ+bXrxuzLG48
                script: |
                    cd www
                    git clone git@github.com:arndelp/LiveEventsBackEnd.git
                    ls
                    
                   
                    

