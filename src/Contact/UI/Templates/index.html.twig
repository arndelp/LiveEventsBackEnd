{# héritage du template de base #}
{% extends 'twig_heritage/template.html.twig' %}

{% block title %}Boite de réception{% endblock %}

{% block body %}  
<h1>Boite de réception</h1>
    <div class="row">
        <table class="table table-striped">
                <thead>
                    <tr>
                        <th scope="col" class="tableauPoi">id</th>                            
                        <th scope="col" class="tableauPoi">e-mail</th>
                        
                    </tr>
                </thead>
            {% for contact in contacts %}
            <tbody>
                <tr>
                    <th class="tableauPoi" scope="row">{{ contact.id }}</th>
                    <td>{{ contact.email }}</td>
                    <td>
                        <div class='col'>
                            <form action="{{ path('contact.detail', {id: contact.id}) }}">
                                <button type="submit"  class="btn btn-secondary btn-sm" class="card-link">Voir</button>
                            </form>   
                            </div>    
                        </td>   
                        <td class="tableauPoi">
                            <div class='col'>
                                <form action="{{ path('contact.delete', {id: contact.id}) }}">
                                    <button type="submit"  class="btn btn-danger btn-sm" class="card-link">Supprimer</button>
                                </form>   
                            </div>    
                        </td>   
                </tr>
            </tbody>

            {% endfor %}
        </table>

{# Pagination #}
    {% if isPaginated and nbrePage > 0 %}
        <div class="row mt-3">
            <nav aria-label="Page navigation">
                <ul class="pagination">             

                    {% set queryParams = app.request.query.all() %}

                    {# Bouton Précédent #}
                    {% if page > 1 %}
                        {% set prevParams = queryParams | merge({'page': page - 1, 'nbre': nbre}) %}
                        <li class="page-item">
                            <a class="page-link" href="{{ path('contact.list.alls', prevParams) }}">Précédent</a>
                        </li>
                    {% endif %}

                    {# Pages #}

                    {% set start = max(page - 1, 1) %}
                    {% set end = min(page + 1, nbrePage) %}

                    {% for p in range(start, end) %}
                        {% set pageParams = queryParams | merge({'page': p, 'nbre': nbre}) %}
                        <li class="page-item {{ p == page ? 'active' : '' }}">
                            <a class="page-link" href="{{ path('contact.list.alls', pageParams) }}">{{ p }}</a>
                        </li>
                    {% endfor %}

                    {# Bouton Suivant #}
                    {% if page < nbrePage %}
                        {% set nextParams = queryParams | merge({'page': page + 1, 'nbre': nbre}) %}
                        <li class="page-item">
                            <a class="page-link" href="{{ path('contact.list.alls', nextParams) }}">Suivant</a>
                        </li>
                    {% endif %}          
                    
                </ul>
            </nav>
        </div>
    {% endif %}

    </div>
{% endblock %}