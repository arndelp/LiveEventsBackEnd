{% extends 'twig_heritage/template.html.twig' %}

{% block title %}Liste des POI wc/Restauration{% endblock %}

{% block body %}    
{# affichage de toutes les personnes de la base de données #}
    <div class="row">
        <table class="table table-striped">
            <thead>
                <tr>
                    <th scope="col">Nom</th>                            
                    <th scope="col">Latitude</th>
                    <th scope="col">Longitude</th>
                    <th scope="col">Icone</th>
                    <th scope="col">Largeur</th>
                    <th scope="col">Hauteur</th>
                    
                </tr>
            </thead>

            {% for wc in wcs %}

            <tbody>
                    <tr>  
                        <th scope="row">{{ wc.name }}</th>                 
                        <td>{{ wc.position.lat }}</td>
                        <td>{{ wc.position.lng }}</td>
                        <td>
                            <div>
                                <img src= "{{ asset('uploads/markers/' ~ wc.iconId) }}" height= 25em></td>
                            </div>
                        <td>{{ wc.width }}</td>
                        <td>{{ wc.height }}</td> 
                       
                        <td>
                            <div class='col'>
                                <form action="{{ path('wc.edit', {id: wc.id}) }}">
                                    <button type="submit"  class="btn btn-secondary btn-sm" class="card-link">Modifier</button>
                                </form>   
                            </div>    
                        </td>   
                        <td>
                            <div class='col'>
                                <form action="{{ path('wc.delete', {id: wc.id}) }}">
                                    <button type="submit"  class="btn btn-danger btn-sm" class="card-link">Supprimer</button>
                                </form>   
                            </div>    
                        </td>                                              
                    </tr>
            </tbody>
            
            {% endfor %}

        </table>

    </div>
{% endblock %}

